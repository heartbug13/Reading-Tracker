spring.application.name=Backend

#spring.datasource.url=jdbc:postgresql://localhost:5432/readingtracker
#spring.datasource.username=postgres
spring.datasource.url=jdbc:postgresql://aws-1-us-east-2.pooler.supabase.com:5432/postgres?user=postgres.yripynylkzzcncmkqnow&password=${APP_PASSWORD}
spring.datasource.username=postgres
spring.datasource.password=${APP_PASSWORD}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

api.service.key=${GOOGLE_BOOKS_API_KEY}

# JWT secret key
jwt.secret.key=${JWT_SECRET_KEY}
jwt.expiration-time=3600000

spring.config.import=optional:file:.env[.properties]

